# Configurable-Vendor-Approval-Workflow-System
This project implements a configurable workflow decision system for vendor approval.
The system processes vendor applications, evaluates business rules, interacts with simulated external dependencies, tracks workflow state, and records audit logs.

The goal is to demonstrate a resilient workflow platform capable of handling real-world business processes under changing requirements.
